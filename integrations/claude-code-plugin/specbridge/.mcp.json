{
  "mcpServers": {
    "specbridge": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/dist/mcp-server.cjs",
        "--stdio",
        "--project-root",
        "${CLAUDE_PROJECT_DIR}"
      ]
    }
  }
}
