# SpecBridge for Claude Code

This thin integration teaches Claude Code how to conduct a SpecBridge 2.0 design conversation and connects it to the canonical `specbridge mcp` command.

Install SpecBridge 2.0 so `specbridge` is available on `PATH`, then install this plugin. It contains no bundled runtime, coding-agent launcher, worktree manager, or implementation supervisor.
